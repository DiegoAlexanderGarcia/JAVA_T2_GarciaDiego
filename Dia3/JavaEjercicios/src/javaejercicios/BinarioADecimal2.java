package javaejercicios;

import java.util.Scanner;

public class BinarioADecimal2 {
    Scanner sc = new Scanner(System.in);

        System.out.print("Ingrese un número binario (como entero): ");
        int binario = sc.nextLine();

        int decimal = 0; // Almacena el resultado final
        int posicion = 0;
        
    for (int i = binario.length()-1; i>=0;i--) {
            char digito = binario.charAt(1);
            if (digito == '1'){decimal += Math.pow(2, potencia);
            }
            posicia++;
}
            binario;
        }
}
